query GetCartItems {
  getCartItems {
    cartItems {
      id
      quantity
      createdAt
      updatedAt
      product {
        id
        name
        price
        description
        discountPercentage
        offerPrice
        imageUrl
        brand
        stockCount
      }
    }
  }
}
